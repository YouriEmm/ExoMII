{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bienvenue sur votre tableau de bord</h1>

    {% if app.user %}
        <p>Voici vos exercices disponibles :</p>
        <ul>
            {% for exercice in exercices %}
                <li>{{ exercice.titre }} - <a href="{{ path('exercice_show', {'id': exercice.id}) }}">Voir l'exercice</a></li>
            {% endfor %}
        </ul>
    {% else %}
        <p>Vous devez vous connecter pour accéder aux exercices.</p>
    {% endif %}
{% endblock %}
